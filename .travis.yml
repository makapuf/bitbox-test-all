{
  "sudo": "required",
  "dist": "trusty",
  "language": "c",
  "before_script": [
    "sudo apt-get -qq update",
    "sudo -H make ubuntudeps",
    "pip install Pillow"
  ],
  "script": [
    "make test_allemu"
  ],
  "env": "BITBOX=$TRAVIS_BUILD_DIR/bitbox",
  "group": "stable",
  "os": "linux"
}

